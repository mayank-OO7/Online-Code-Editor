<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- google font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Mono:ital@1&display=swap" rel="stylesheet">

    <!-- bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    
    <!-- css -->
    <link rel="stylesheet" href="/styles.css">

    <title>Online Code Compiler</title>
  </head>
  <body>

    <form method="post" action="/">

      <header class="row">

        <div class="col col-md-6 col-12 main-heading">
          <h2>Online Code Compiler</h2>
        </div>

        <div class="col col-md-6 col-12 buttons">

          <select class="language" name="language" id="" >
            
            <option value="c"
                <%if(language==="c"){%>
                    selected
                <%}%>
            >C</option>

            <option value="cpp17"
                <%if(language==="cpp17"){%>
                    selected
                <%}%>
            >C++17</option>

            <option value="php"
                <%if(language==="php"){%>
                    selected
                <%}%>
            >PHP</option>

            <option value="java"
                <%if(language==="java"){%>
                    selected
                <%}%>
            >Java</option>

            <option value="python3"
                <%if(language==="python3"){%>
                    selected
                <%}%>
            >Python3</option>

            <option value="ruby"
                <%if(language==="ruby"){%>
                    selected
                <%}%>
            >Ruby</option>

            <option value="nodejs"
                <%if(language==="nodejs"){%>
                    selected
                <%}%>
            >nodejs</option>

          </select>

          <button class="btn btn-light run-btn" type="submit">Run</button>

        </div>

      </header>

      <div class="row">
        
        <div class="col col-md-6 col-12">
          <textarea name="code" placeholder="Code here..." ><%=code%></textarea>
        </div>

        <div class="col col-md-6 col-12">
          <textarea name="input" placeholder="Input"><%=input%></textarea>
        </div>

      </div>

    </form>

    <div class="output">
      <h5>Output:</h5>
      <p><%=output%></p>
    </div>

  </body>

</html>
